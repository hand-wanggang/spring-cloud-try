server:
  port: 8086
spring:
  application:
    name: config-client
  cloud:
    config:
      name: config-client
      uri: http://localhost:8085/server-config/
      label: master
      profile: dev